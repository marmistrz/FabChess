[workspace]
members = [
    "core-sdk",
    "extended-sdk",
    "uci-engine",
    "tuning",
    "referee",
    "playground",
    "benchmarking",
    "tests"
]
default-members = ["uci-engine"]
[profile.release]
opt-level = 3
lto = true